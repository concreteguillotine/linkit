<header>
    <h3> New Reply... Be Nice! </h3>
</header>

<%= form_with model: @comments, local: true do |f| %>
    <div>
        <%= f.text_area :text, label: "reply" %>
    </div>
        <%= f.submit %>
<% end %>